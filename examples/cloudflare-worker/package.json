{
  "name": "easymatrix-cloudflare-worker-example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy:dry-run": "wrangler deploy --dry-run --outdir bundled"
  },
  "dependencies": {
    "@beeper/easymatrix-cloudflare": "workspace:*",
    "easymatrix": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.85.0"
  }
}
