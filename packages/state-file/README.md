# @better-matrix-js/state-file

Filesystem state adapter for `better-matrix-js`.

```ts
import { createFileMatrixStore } from "@better-matrix-js/state-file";

const state = createFileMatrixStore(".matrix-state/alice");
```
